<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lm.ll.spark">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_6park"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_6park"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ArticleDisplayActivity" />
        <activity
            android:name=".EliteEroticaArticleListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FavoritedNewsListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ClassicEroticaArticleListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DisplayArticleActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen"
            android:theme="@style/FullscreenTheme"></activity>
    </application>

</manifest>